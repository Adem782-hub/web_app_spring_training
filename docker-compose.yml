version: '3.8'

services:
  todolist:
    build: .  # Utilise le Dockerfile dans le même répertoire
    ports:
      - "8081:8080"  # Redirige le port 8081 de la machine hôte vers le port 8080 du container

